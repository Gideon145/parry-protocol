[phases.setup]
nixPkgs = ["nodejs_20", "curl", "bash"]

[phases.install]
cmds = [
  "curl -sSL https://raw.githubusercontent.com/okx/onchainos-skills/main/install.sh | sh",
  "cd agent && npm ci"
]

[phases.build]
cmds = ["cd agent && npm run build"]

[start]
cmd = "node agent/dist/index.js"
